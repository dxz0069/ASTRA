# ASTRA·星辰 项目总览

> 更新：2026-08-29（v0.2 FGS 重建后）｜ 本文档是项目的单一入口：定位、架构、版本演进、运维体系、产物清单。

## 术语对照（ASTRA 北斗导航体系 ↔ 协议名）

> 命名取自中国古代星官：**玉衡权衡定向，摇光出击巡天；星官沿途记天枢、偶见客星、
> 辅星随行、逐宿而驻，终抵北辰。**（协议层 API/代码保持英文，不随品牌变）

| ASTRA 古星名 | 协议名 | 天文出处 | 含义 |
|---|---|---|---|
| 星图 | FGS graph | — | Fact–Goal–Step 图（append-only 唯一共享状态） |
| 天枢 | Fact | 北斗第一星，枢者根基 | 已确认事实（负结果同权） |
| 北辰 | Goal | 北极星古称，众星共之 | 完成条件 = 搜索终止条件 |
| 星宿 | SubGoal | 二十八宿，月行逐宿停驻 | 动态子目标（航程驿站） |
| 斗柄 | Step | 斗柄东指，天下皆春 | 因果行动（带预期天枢 expect；可关闭留痕） |
| 客星 | Finding | 史记载新现天体为客星 | 沿途发现（漏洞类一等产出） |
| 辅星 | Hint | 开阳旁辅星，助目辨星 | 外部注入判断 |
| 玉衡 / 摇光 / 角宿 | Decide / Execute / Bootstrap | 璇玑玉衡以齐七政 / 斗柄之梢 / 周天第一宿 | 双活动 + 首次执行 |
| 星官 | worker | 史记·天官书 | 巡天观测记录的执行者 |
| 归航 / 巡航 | conclude / 进行中 | — | 斗柄收束 / 未收束态 |


## 一、定位

**经验复利型自主安全智能体**。核心命题：让安全 Agent 拥有记忆、越用越强——正面经验给方向、失败经验防重蹈、赛中实时生长、赛后自动结晶。区别于"更强单次执行"路线（学界已证其架构随模型升级而收敛），ASTRA 押注模型不会自己变强的能力：记忆、验证、调度、经验复利。

## 二、架构

```
┌────────────────────────────────────────────────────┐
│ 认知层·焦点上下文（v0.2：读侧治理）                   │
│  相关度+语义召回+时间近度+图距加成 · 关键事实钉住     │
│  · context_budget 硬上限零膨胀 · 全图文件引用        │
│  （记忆管线 L4 攻击链库/避坑库/同题型注入保留，       │
│   托管合规 ASTRA_KB_DISABLED=1 默认禁用）            │
├────────────────────────────────────────────────────┤
│ 决策层·FGS 导航引擎（v0.2）                           │
│  Fact/Goal/Step(+expect/status/close 账本) 三原语    │
│  + Finding(沿途发现) + SubGoal(动态子目标)           │
│  · Decide 串行·事件触发·干净上下文·只操作图          │
│  · Execute 并发多实例·submit_fact 自证入图           │
│  · append-only 无审查环无压缩 · 决策链可回放         │
├────────────────────────────────────────────────────┤
│ 执行层·pi 舰队（v0.2：唯 pi 底座）                    │
│  deepseek-flash Execute×2(吞吐主力) +               │
│  GLM-5.3 Decide(深思考,同图串行跨图并行)            │
├────────────────────────────────────────────────────┤
│ 自愈层·四层防御（无人值守生命线）                     │
│  网络瞬断题不死 → 泄漏容器清道夫 → 全停看门狗        │
│  → 半死看门狗(图零增长即自重启, 盯 pi 会话)          │
└────────────────────────────────────────────────────┘
```

## 三、版本演进（关键里程碑）

| 版本 | 内容 |
|---|---|
| V1-V2 | 星图引擎、任务管线（bootstrap/reason/explore/consolidate）、双星审查、知识库注入、期望预算、defer 梯子 |
| R5 | 托管实战修复：defer 僵尸接线、对账扫描、围栏解析（10850 分记录轮） |
| V3 | 四层记忆升级启动：语义召回（embeddings 降级回退）、回忆强化/遗忘统计、L4 自动固化、astra memory stats |
| V4 | 举一反三（同题型邻居注入）、赛中实时复利（live memory reload） |
| V5 | 失败经验库（负记忆）、astra memory map 星图可视化、赛后蒸馏流水线三件套 |
| V6 | 焦点检索结构信号：关键事实钉住、图距检索（未决航向锚点） |
| V7 | 论文对照七件套：状态卡片、跨模型评审、UCB 航向投入卡、主题聚簇压缩+修订、TDI 难度指数、证据自复验契约、Constellation 跨项目侦察共享 |
| 自愈版 | 四层防御 + resume 死锁根治（Tsecbench 实弹调优期产出，事故档案见下方） |

## 四、运维与文档地图

| 文档 | 内容 |
|---|---|
| `docs/本地调优作战记录.md` | 四起实战事故档案（根因链/解法/SOP）——可靠性工程的一手素材 |
| `docs/答辩一页纸-痛点特色差异化.md` | 十六件进化机制话术（本地） |
| `docs/答辩路演方案.md` | 15 分钟路演全包（本地） |
| `docs/优化路线图-架构对照论文.md` | 架构特点 × 论文对照（本地） |
| `docs/调研-2026安全与Agent记忆前沿.md` | PentestGPT v2/Infini/CTFExplorer 等调研（本地） |
| `docs/作品简介.md` `docs/技术方案.md` `docs/团队介绍.md` | 对外发布物（随仓库公开） |
| `tools/run_status.py` | 跑分一站式态势板 |
| `tools/merge_knowledge.py` `tools/distill_review.py` `tools/check_pro_activation.py` | 赛后流水线（固化/蒸馏/Pro验证） |

## 五、运行形态

- **本地模式**：`dist/local.env`（token/密钥/舰队配置，v0.2 起 PI_* 键）+ `astra/.venv` 起 runner，状态板 `py -3 tools/run_status.py`；重启 SOP 见作战记录。
- **托管模式**：v0.2 镜像待重建打包（pi@0.73.0 底座）；上一代可发车包 `dist/app_dist_v7.tar`（claudecode 栈，R10）。环境变量：网关 `.tsecbench.gw` 规则、`ASTRA_KB_DISABLED=1`、`PI_MODEL/PI_BASE_URL/PI_API_KEY/PI_PROVIDER_API`（GLM 决策通道 `ZHIPU_PI_*`）、`ASTRA_DECIDE_TIMEOUT`。
- **合规红线**：平台规则禁止内置题目历史记忆——镜像物理空壳 KB + 运行时开关双保险；密钥只走环境变量，绝不入镜像/git（dist/ 已 ignore）。

## 六、测试与质量

- 全量回归 `astra/tests`（v0.2：148 项全绿，含 FGS 契约、finding/步骤关闭/子目标流、CLI（trace/map/report）、上下文预算、调度逻辑、pi 舰队渲染/清理/用量）。
- 测试隔离纪律：fake 跑分写 memory-stats 已指向临时目录（曾污染真实统计，已修）。

## 七、当前进行时

v0.2 FGS 重建已完成（分支 `astra-fgs`，148 测试绿）；发车前三步待做：pi 双网关 spike → 镜像重建+容器冒烟 → 打包 v8。R9（13464 轮）日志复盘完成并驱动了 R10；R10 托管轮结果待导出。

## 八、V8 满血升级（2026-08-27）

基于 Tsecbench 榜首 agent 对话日志逆向拆解 + 代码审计 P0/P1 全量修复。

**架构升级**：
- 写时治理替代读时裁剪（6 个 prompt 文件重构：收束契约/规划者协议/流式提交/负结果保留/元博弈）
- 负结果一等公民全链路（ConcludeRequest→Server→Client→explore 自动检测→焦点加权保活）
- P0：execv 自愈重启前优雅关引擎（shutdown + killpg + 端口释放等待）
- P1×5：队列竞态丢题/Session 泄漏/孙进程孤儿/图快照膨胀/dispatcher 字典无界增长

**论文叙事**：读时治理 vs 写时治理的上下文工程对比（有对手 383M cache_read 实测数据支撑）；
记忆系统补齐榜首架构的跨 run 失忆弱点（差异化贡献）。

**镜像 v5**：agent-slim-v5.tar.gz（2.8G，sha256 a2a34524…），198 tests 全绿。

## 九、深度安全审计第二轮（2026-08-27）

三路并行审计（服务端API安全 / 调度器逻辑闭合 / Runner数据操作），发现并修复：

**安全加固**（服务端暴露场景）：
- API-Key 认证中间件（ASTRA_AUTH_TOKEN 环境变量启用，覆盖全部路由）
- PUT /settings 加上限（防 intent_timeout 劫持瘫痪全服务）
- 所有 str 字段加 max_length / archive 分批（防 OOM / SQL 变量溢出）
- kind 字段 Literal 枚举 / 生产禁用 /docs /openapi.json
- 409 响应去 claimer 名（防 worker 身份枚举）

**调度器核心修复**：
- D1: runtime_project_ids 只增不减 → max_running_projects 饿死新项目（reap 时移除）
- D2: reason 审查后写图前不查租约 → 双重写图（写前复查 lease.failure）
- D3: 心跳线程裸 json() → 静默死亡 → 同 intent 双跑（try/except + 降级）
- D4: consolidate 归档未保护未决航向 from 引用 → 来源链断裂
- D6: archive 失败仍返回 success → 每周期叠摘要风暴
- J2: unhealthy 冷却被同批 success 清除

**Runner 核心修复**：
- D1(最严重): self_heal_restart 的 shutdown 是死代码（直接调 AstraDaemon.instance().shutdown()）
- D2: 脉搏基线从历史峰值改为上次采样值（防活跃题换血后假阳性重启）
- D3: 看门狗模块级状态跨实例残留（_reset_watchdog_state() 在 run_benchmark 入口调用）
- D4: Constellation 写入加 threading.Lock
- D5: ProgressStore 损坏时尝试 .tmp 回退
- B1: started_this_round 局部变量（防跨轮残留假 close_failed）
- B2: close 清道夫跳过活跃题（防杀正在解题的容器）
- B3: skipped_codes() 加锁


## 十、v0.2 FGS 重建（2026-08-29）

对齐榜首闭源方案路线（`docs/榜首闭源架构解读与FGS提炼.md`），当日完成（分支 `astra-fgs`，+2914/-4988 行）：

- **架构**：星记/航向 → **FGS 图**（Fact/Goal/Step+Finding/SubGoal，append-only）；reason/explore → **Decide/Execute** 双活动（同一 runner 两种注入）；**整链删除**双星审查、consolidate 压缩、置信度/证据/质询字段。
- **底座**：claudecode/codex/dsh 适配器全部移除，**仅 pi**（`@mariozechner/pi-coding-agent@0.73.0`）；舰队 DS-flash Execute×2 + GLM-5.3 Decide；watchdog/自检/用量/pkill 全链 pi 路径。
- **提示词**：9 个 495 行 → 5 个 224 行（原版上游骨架 + FGS 语义，零安全耦合）；安全方法论全部下沉 AGENTS.md 任务层。
- **保留的 ASTRA 特色**：赛制经济学层（价值排序/defer 预算/hint 门控/多旗收割+回执入图）、四层自愈、记忆管线（KB 禁用中）、23 技能库、重工具链镜像、local 模式、星图可视化——一项未丢。
- **新增能力**：Finding 沿途发现（SRC/审计计分物）、Step expect（预期产出契约）+ close 死路账本、SubGoal、Decide 干净上下文（决策成本 O(图)）。
- 详见 `note/release/0.2.0.md` 与 `docs/整改计划-FGS路线.md`。
