# ASTRA 0.2.0（cairn-y）

> 2026-08-29。对齐 Cairn_Y 路线（l3yx《众人向左，我偏向右》）的架构重建：FGS 图 + Decide/Execute 双活动 + 仅 PI 执行底座 + 极短通用提示词。分支 `cairn-y`，7 提交，净删 ~2000 行。

## 依据

- 用户指令三条：底座只留 PI（弃 claudecode/codex/dsh）；技术栈向 Node 演进（PI 生态）；内置提示词极短、与安全任务解耦。
- l3yx 公众号文章逐字解读（`docs/Cairn_Y文章解读与架构提炼.md`）：Cairn_Y = FGS 图（Fact-Goal-Step+Finding，append-only）+ Decide（串行/事件触发/干净上下文/只操作图）+ Execute（世界工具+submit_fact 自证入图）+ 无审查/无压缩/无 Skill/RAG/MCP。

## 变更清单

### 提示词（0e0920c）
- 9 个模板 495 行 → 5 个 218 行：原版上游骨架（提取自仓库首提交 dc24d27）+ FGS 术语。
- 新集：bootstrap（流式 submit_fact）/ bootstrap_conclude / execute / execute_conclude / decide。
- 删除：challenge / verdict（双星审查）、consolidate（星尘压缩）、platform-recon（未接线）。
- 安全方法论全部下沉到任务描述层（AGENTS.md / runner 注入），引擎提示词 0 安全耦合。

### server 层（ebec3b7）
- 数据模型：Intent → **Step**（+`expect` 预期产出、`status` open/closed、`close_reason` 留痕）；新实体 **Finding**（沿途发现）与 **SubGoal**（动态子目标）。
- Fact 瘦身：删 confidence/evidence/challenged（审查链遗产）；kind=summary 并入 regular。
- API：/intents→/steps、/reason/*→/decide/*、新增 /steps/{id}/close、/findings、/subgoals；settings 字段 step_timeout/decide_timeout。
- 旧库迁移：intents→steps 数据全量搬迁，审查字段丢弃。

### dispatcher 核心（7c915b6）
- tasks/reason→**decide**（串行租约、干净上下文、输出 complete/steps+close_steps+subgoals/drop_subgoals）；tasks/explore→**execute**（submit_fact + 可选 finding）。
- **整链删除**：双星审查（质询+裁决+机器预审+低置信质询）、consolidate 任务、archive_facts、REVIEW_HINT。
- 适配器层：删 claudecode.py/codex.py/_curl.py，`WorkerType = pi | mock`。

### 引擎/镜像（e551bbc、409167f）
- 舰队：`deepseek-execute-{N}`（pi/DS，p0，maxrun3）+ `glm-decide`（pi/GLM，p1）或 `deepseek-decide` 兜底；env 全面 PI_*（PI_PROVIDER_API 默认 anthropic）。
- watchdog/自愈/usage/pkill 全部改盯 `$TMP/astra-pi/*/sessions/**.jsonl`；ASTRA_DECIDE_TIMEOUT（回读旧名 ASTRA_REASON_TIMEOUT）。
- Dockerfile(.slim)：装 `@mariozechner/pi-coding-agent@0.73.0`，卸 claude-code/playwright-mcp/.claude/CLAUDE.md。

### 前端（1699ee8）
- 步骤/事实/子目标/沿途发现展示；删置信度/质询/证据 UI；设置项对齐 step/decide 超时。

### 测试（a4a958e）
- 197 → 148 用例全绿：删审查/压缩/v7 专测，新增 finding 提交、step 关闭留痕、subgoal 流、decide ops 契约、pi 舰队渲染/清理/用量。

## 明确保留（赛制适配件，与文章不冲突）

价值排序 / defer 预算 / hint 购买 / 多旗收割 / 平台回执入图 / 四层自愈 watchdog / 工具安装类环境预备 / KB（ASTRA_KB_DISABLED=1 默认禁用）。

## 待办（下一轮）

- pi 双网关 spike（DS/智谱 anthropic 实测：headless json、会话续接、执行场景稳定性）
- 镜像重建 + 容器内冒烟
- 打分包 + 托管轮验证（观察指标：每题调用数/token 对比 CC 税）
- 阶段 C：Node 引擎（PI 库嵌入），见 `docs/整改计划-对齐CairnY.md`
